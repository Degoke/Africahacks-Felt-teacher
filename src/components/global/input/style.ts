import { makeStyles, createStyles } from '@material-ui/core/styles'

export default makeStyles(() =>
  createStyles({
    border: {
      borderRadius: '25px',
    },
    label: {
      color: '#000000',
    },
  })
)
